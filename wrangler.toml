name = "list-draft"
compatibility_date = "2026-09-18"
pages_build_output_dir = "dist"

# Create once: npx wrangler d1 create list-draft
# Then paste the id here. Local `wrangler pages dev` works without it.
[[d1_databases]]
binding = "DB"
database_name = "list-draft"
database_id = "00000000-0000-0000-0000-000000000000"
