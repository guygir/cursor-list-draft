name = "list-draft"
compatibility_date = "2026-09-18"
pages_build_output_dir = "dist"

# Optional. Production can bind D1 in the Cloudflare dashboard instead.
# CLI: npx wrangler d1 create list-draft — then paste the id.
[[d1_databases]]
binding = "DB"
database_name = "list-draft"
database_id = "00000000-0000-0000-0000-000000000000"
